#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Format & lint only staged JS files
npx lint-staged

# Run Jest tests before allowing commit
npm test

echo "✅ All checks passed! Commit allowed."